.row
  = render 'shared/notice'
.row
  - @rooms.each do |r|
    .large-6.columns
      .row
        = render partial: 'rooms/room_bar', locals: { room: r, full_view: false  }
      .row.room
        = render partial: "rooms/room", locals: { room: r}